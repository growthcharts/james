% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/request_tab10.R
\name{request_tab10}
\alias{request_tab10}
\title{Request a tab10 session URL for personalised risk communication}
\usage{
request_tab10(
  txt = "",
  sitehost = "",
  tab10host = "",
  session = "",
  format = "3.0",
  ...
)
}
\arguments{
\item{txt}{A JSON string, URL, or file with BDS data in JSON format. Data
should conform to the BDS JGZ 3.2.5 specification.}

\item{sitehost}{The JAMES server that receives the upload (the same host
serving this very endpoint). Defaults to \code{"http://localhost:8080"} if not
specified.}

\item{tab10host}{The server hosting the \code{tab10} Shiny app, e.g.
\code{"https://james.groeidiagrammen.nl/tab10"}. Defaults to \code{sitehost} with a
\verb{/tab10} path appended.}

\item{session}{Optional session key if data is already uploaded to
\code{sitehost} (e.g. by a prior \code{\link[=request_site]{request_site()}} call). When given, \code{txt} is
ignored and no new upload happens -- \code{tab10} retrieves the data itself via
the existing session's \verb{/rda} path, so the same session key already used
for \verb{/site} can be handed to \code{tab10} as-is.}

\item{format}{JSON schema version, e.g., \code{"3.0"}. Used when uploading.}
}
\value{
A character string URL pointing to the \code{tab10} app with a
\verb{?session=} query parameter, or, if no session could be obtained, the bare
\code{tab10host} URL.
}
\description{
Uploads BDS data to the server (reusing the same upload mechanism as
\code{\link[=upload_data]{upload_data()}}) and constructs a URL to a standalone \code{tab10} deployment
with the resulting session key attached. Callers (e.g. \code{jamesapp}) use the
returned URL to hand off control to \code{tab10}, which retrieves the uploaded
data itself via the session's \verb{/rda} path.
}
\examples{
fn <- system.file("testdata", "client3.json", package = "james")
\dontrun{
url <- request_tab10(txt = fn, sitehost = "http://localhost:8080")
url
}
}
\seealso{
\code{\link[=request_site]{request_site()}}, \code{\link[=upload_data]{upload_data()}}
}
\author{
Stef van Buuren 2026
}
\keyword{server}
