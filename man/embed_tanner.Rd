% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embed_tanner.R
\name{embed_tanner}
\alias{embed_tanner}
\title{Draw an interactive, embeddable Tanner pubertal-stage chart}
\usage{
embed_tanner(
  txt = "",
  session = "",
  format = "1.0",
  sex = NULL,
  traces = "",
  stage_lines = "",
  patient_id = NULL,
  ...
)
}
\arguments{
\item{txt}{A JSON string, URL, or file with BDS data in JSON format. Data
should conform to the BDS JGZ 3.2.5 specification.}

\item{session}{OpenCPU session key with the uploaded data}

\item{format}{JSON schema version, e.g., \code{"3.0"}. Used when uploading.}

\item{sex}{Optional. \code{"male"} or \code{"female"}. Overrides the sex derived
from the target's persondata when supplied; see Details.}

\item{traces}{Optional. Comma-separated raw Tanner \code{varName}s (see
Details) whose patient-data trace starts visible.}

\item{stage_lines}{Optional. Comma-separated raw Tanner \code{varName}s
(see Details) whose reference lines start visible.}

\item{patient_id}{Optional. Overrides the patient id shown in the
chart title ("Tanner pubertal stages - Patient \if{html}{\out{<id>}}") -- both the id
derived from the uploaded data's persondata and bdsreader's -1
"no id" sentinel (which is otherwise suppressed, see Details). Pass
\code{NA} to force the title back to its no-id form.}

\item{...}{Ignored}
}
\value{
A length-1 character string: the self-contained HTML document.
}
\description{
\code{embed_tanner()} is the Tanner-chart counterpart to \code{\link[=embed_chart]{embed_chart()}}:
it returns a self-contained HTML document (a \code{plotly} widget) as a
character string, retrievable at the session's \code{R/.val/text} path and
meant for embedding via \verb{<iframe srcdoc="...">}. Unlike \code{embed_chart()},
there is exactly one chart, so there is no \code{chartcode}/multi-chart
dispatch.
}
\details{
When \code{txt}/\code{session} yield no target data, or the target has no
pubertal measurements at all, the chart still renders -- just the
reference frame, with no patient trace -- rather than the panel being
hidden or the call failing.

The chart title includes the patient id (e.g. "... - Patient 1234")
whenever one is available. bdsreader codes an unset client id as -1,
not \code{NA}; that sentinel is detected and treated as no-id, so the
title correctly omits the suffix rather than showing "- Patient -1".
Use \code{patient_id} to override the id shown regardless of what the
uploaded data carries.

\code{sex} defaults to \code{NULL}, which derives sex from the target's own
persondata (falling back to \code{"male"} when there is no target at all).
Pass \code{"male"}/\code{"female"} explicitly to override that -- e.g. jamesapp's
Tanner panel follows the UI's geslacht radio button rather than
whatever sex happens to be in the uploaded data, so a user can preview
the reference frame for either sex regardless of the patient record.

\code{traces}/\code{stage_lines} control which stage types' patient-data traces
and reference lines, respectively, are visible when the chart first
renders -- see \code{tannerly::plot_stadia_ly()}. Both are comma-separated
strings of raw Tanner \code{varName}s (\code{"gen"}, \code{"phb"}, \code{"tv"} for male
patients; \code{"bre"}, \code{"phg"}, \code{"men"} for female), since this endpoint
takes multipart/form-data rather than R vectors. Leaving either \code{""}
(the default) keeps \code{plot_stadia_ly()}'s own defaults: all three
traces visible, no reference lines. A type not listed still gets a
legend entry -- viewers can always toggle it on/off by hand.
}
\examples{
fn <- system.file("testdata", "client_tanner.json", package = "james")
html <- embed_tanner(txt = fn)
html <- embed_tanner(txt = fn, sex = "female")
html <- embed_tanner(txt = fn, traces = "gen", stage_lines = "gen")
html <- embed_tanner(txt = fn, patient_id = "1234")
}
\seealso{
\code{\link[=embed_chart]{embed_chart()}}
}
\author{
Stef van Buuren 2026
}
\keyword{server}
